// this file will generate the foamtree visualization of the file/folder structure

const generate = () => {
  // we should return the final result here, or maybe an object with a visual and non-visual option
};

module.exports = { generate };
